# Database Configuration

# Database Type: sqlite | postgresql | mysql
# Default: sqlite (if not specified or if URL implies it)
DATABASE_TYPE=sqlite

# Database Connection URL
# SQLite: file:local.db or libsql://...
# Postgres: postgres://user:pass@host:port/db
# MySQL: mysql://user:pass@host:port/db
DATABASE_URL=file:local.db

# Auth Token (Required for Turso/LibSQL remote)
# DATABASE_AUTH_TOKEN=your-token-here

# Environment: development | production
DENO_ENV=development
